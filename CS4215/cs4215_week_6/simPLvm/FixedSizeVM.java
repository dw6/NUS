package simPLvm;

public class FixedSizeVM {
	
	// the size of the heap is fixed
	
   public static int HEAP_SIZE = 100;
   
	// the size of the runtime stack is fixed, too

	static final int RUNTIMESTACK_SIZE = 10000;
}
